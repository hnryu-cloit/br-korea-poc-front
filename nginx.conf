server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # 메뉴 이미지 직접 서빙 (백엔드 반환 경로와 일치)
    location /images/ {
        root /usr/share/nginx/html;
        autoindex off;
    }
}

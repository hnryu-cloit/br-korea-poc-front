server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # 메뉴 이미지 직접 서빙
    location /static/menu-images/ {
        alias /usr/share/nginx/html/menu-images/;
        autoindex off;
    }
}
